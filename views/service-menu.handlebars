<div class = "container">
    <div class = "wrapper">
        <select name="Services" id="servicesDropdown">
            <option value="none">Select the service you need</option>
            {{#each servicemenus}}
            <option value={{title}}>{{title}} will take {{manHour}} hours</option>
            {{/each}}
        </select>


    </div>
</div>
<div class = "container">
    <div class = "wrapper">
        <form>
            <input type="month" min = "2020-01" id ="serviceCalendar">
            <input type="submit" value="Select a date">
        </form>
    </div>
</div>